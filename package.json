{
  "name": "model-arena",
  "version": "1.0.0",
  "description": "Multi-model LLM comparison PoC on DigitalOcean — fight model FOMO with combined inference.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test \"test/*.test.js\"",
    "smoke": "node test/smoke.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
